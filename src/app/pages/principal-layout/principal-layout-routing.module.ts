import { NgModule } from '@angular/core';
import { OlMapComponent } from 'src/app/components/map/ol-map/ol-map.component';
import { RouterModule, Routes } from '@angular/router';

const routes: Routes = [
 
];

@NgModule({
  declarations: [],
  imports: [
    RouterModule.forChild( routes )
  ]
})
export class PrincipalLayoutRoutingModule { }
